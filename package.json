{
  "name": "repair-order-tracker",
  "version": "1.0.0",
  "description": "to run the application open two terminals\r in the first terminal run \r ```console\r cd server && npm run dev\r ```\r This will start the server instance.",
  "main": "index.js",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install ",
    "start:client": "cd client && npm run dev",
    "start:server": "cd server && npm run dev",
    "dev": "concurrently --names \"CLIENT,SERVER\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm:start:client\" \"npm:start:server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
